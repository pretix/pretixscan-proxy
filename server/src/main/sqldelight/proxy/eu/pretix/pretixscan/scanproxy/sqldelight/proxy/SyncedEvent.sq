CREATE TABLE SyncedEvent (
    slug text
);

selectAll:
SELECT *
FROM SyncedEvent;

selectBySlug:
SELECT *
FROM SyncedEvent
WHERE slug = ?;

insert:
INSERT INTO SyncedEvent (
    slug
) VALUES (
    ?
);

deleteBySlug:
DELETE FROM SyncedEvent
WHERE slug = ?;

truncate:
DELETE FROM SyncedEvent;

-- for tests only:
testCountAll:
SELECT COUNT(*)
FROM SyncedEvent;

testCountWithSlug:
SELECT COUNT(*)
FROM SyncedEvent
WHERE slug = ?;
